# service/.env.example
# Copy to repo root as .env (recommended) OR place at repo root and keep service/.env.example as template.

# ---- Database (Render Postgres same as ZAI) ----
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DB_SCHEMA=rfq

# ---- Gemini Embeddings ----
GEMINI_API_KEY=
GEMINI_EMBED_MODEL=gemini-embedding-001
EMBED_DIM=1536

# ---- Ingestion controls ----
INGEST_HTTP_TIMEOUT_SEC=60
INGEST_FILE_MAX_MB=40

CHUNK_SIZE=1200
CHUNK_OVERLAP=150

# ---- Glide (Phase 2) ----
GLIDE_API_KEY=
GLIDE_APP_ID=ARzoymvBNIgO6RcvRk7l
GLIDE_MAX_ROWS_PER_CALL=5000

# ---- Drive (Phase 3) ----
GDRIVE_SA_JSON_PATH=

# ---- Logging ----
LOG_LEVEL=INFO